<!DOCTYPE html>
<html>
<head>
  <title>Project2</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>
	<h1>ShotBlog</h1>
	<p><%= current_user ? current_user.username : "(not logged in)" %></p>
	<%= image_tag current_user.avatar.thumb('100x100#').url if current_user && current_user.avatar_stored?%>


	<p><%= current_user ? (link_to "Log Out", logout_path) : (link_to "Log In", login_path) %></p>

	<p><%= current_user ? (link_to "Edit Profile", edit_user_path(current_user)) : (link_to "Sign Up", new_user_path) %></p>

	<% if flash[:error] %>
		<p><%= flash[:error] %></p>
	<% end %>

	<%= yield %>
	
	<hr>Params: <%= params %><hr>

</body>
</html>
